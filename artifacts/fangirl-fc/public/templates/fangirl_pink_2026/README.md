# Template Assets

This folder will contain the card creator template assets:

- `background.png` — full card background image
- `overlay.png` — transparent overlay layer (effects, borders, shine)
- `thumbnail.png` — small preview used in the template picker
- `config.json` — optional per-template configuration (colours, text positions)

Do not add placeholder or fake image files. Add real assets only when they are ready.
